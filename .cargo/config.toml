[language-server.rust-analyzer]
config = { checkOnSave = { command = "clippy" }, cargo = { allFeatures = true }}



[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
