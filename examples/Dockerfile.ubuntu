# Example: Using picolayer with apt-get (Ubuntu/Debian)
FROM ubuntu:22.04

# Copy the picolayer binary
COPY target/release/picolayer /usr/local/bin/picolayer

# Install packages with automatic cleanup
RUN picolayer apt-get curl,git,htop

# Verify the packages were installed
RUN curl --version && git --version && htop --version
