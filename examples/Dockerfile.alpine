# Example: Using microlayer with apk (Alpine)
FROM alpine:3.19

# Copy the microlayer binary
COPY target/release/microlayer /usr/local/bin/microlayer

# Install packages with automatic cleanup
RUN microlayer apk curl,git,htop

# Verify the packages were installed
RUN curl --version && git --version && htop --version
